<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
    android:id="@+id/login_rider"
    android:layout_width="200dp"
    android:layout_height="60dp"
    android:background="@android:color/darker_gray"
    android:text="Login Rider"
    android:textColor="@android:color/white"
    android:layout_marginBottom="100dp"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"/>

    <Button
        android:id="@+id/login_driver"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:background="@android:color/darker_gray"
        android:text="Login driver"
        android:textColor="@android:color/white"
        android:layout_marginBottom="100dp"
        app:layout_constraintStart_toStartOf="@+id/login_rider"
        app:layout_constraintEnd_toEndOf="@+id/login_rider"
        app:layout_constraintBottom_toBottomOf="@+id/login_rider"/>



</androidx.constraintlayout.widget.ConstraintLayout>